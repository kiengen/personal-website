export {};
declare global {
    interface Project {
        name: string,
        photos: string[],
        description: string,
        frameworks: string[],
        dates: string,
        keywords: string[],
    }
}
